name: weather_app
description: Приложение "Погода в кармане"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.4
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  http: ^1.1.0
  #firebase_core: ^3.13.0
  #firebase_auth: ^5.5.2
  #cloud_firestore: ^5.6.6
  #Не работают нихуя

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true